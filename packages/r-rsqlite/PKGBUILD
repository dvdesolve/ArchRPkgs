# Contributor: Viktor Drobot (aka dviktor) linux776 [at] gmail [dot] com

_cranname=RSQLite
_cranver=2.3.1
pkgname=r-${_cranname,,}
pkgver=${_cranver//[:-]/.}
pkgrel=1
pkgdesc="SQLite Interface for R"
arch=(i686 x86_64)
url="https://cran.r-project.org/package=${_cranname}"
license=(LGPL2.1)
depends=('r>=3.1.0' r-bit64 'r-blob>=1.2.0' 'r-dbi>=1.1.0' r-memoise r-pkgconfig 'r-plogr>=0.2.0' 'r-cpp11>=0.4.0' r-methods)
optdepends=(r-callr r-dbitest r-gert r-gh r-hms r-knitr r-magrittr r-rmarkdown r-rvest r-testthat r-withr r-xml2)
source=("https://cran.r-project.org/src/contrib/${_cranname}_${_cranver}.tar.gz")
sha256sums=('9ed23e160c401c14e41c40e9930f72697172b2c72933c2d2725a05e81e1f34ca')

build() {
  R CMD INSTALL ${_cranname}_${_cranver}.tar.gz -l "${srcdir}"
}

package() {
  install -dm0755 "${pkgdir}/usr/lib/R/library"

  cp -a --no-preserve=ownership "${_cranname}" "${pkgdir}/usr/lib/R/library"
}
